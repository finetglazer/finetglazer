#include<bits/stdc++.h>
#define ll long long
using namespace std;
int main()
{
    int n,m;
    while(1)
    {
        cin>>n>>m;
        if(n==0 && m==0) break;
        else{
            ll sum=0;
            sum-=2
        }
    }
}