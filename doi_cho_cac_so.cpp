#include<bits/stdc++.h>
using namespace std;
void het(int a[],int n)
{
    int max=0;
    int min=INT_MAX;
    for(int i=1;i<=n;i++)
    {
        int idx=0;
        int idx2=0;
        if(max<a[i]) idx=i;
        if(a[])
        for(int i=1;i<=n;i++)
        {
            
        }
        
    }
}
int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        int k;cin>>k;
        string s;
        cin>>s;
        int a[10];
        for(int i=1;i<=s.size();i++)
        {
            a[i]=s[i-1]-'0';
        }
        // for(int i=1;i<=s.size();i++) cout<<a[i];
        bool d=1;
    }
}