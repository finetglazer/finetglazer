#include<bits/stdc++.h>
using namespace std;
int main()
{
    // vector<int> v{1,2,3,4};
    // v.erase(v.begin(),v.end());
    // v.push_back(1);
    // for(int i=0;i<v.size();i++) cout<<v[i]<<" ";
    set<string> s;
    string a="heybabybu";
    s.insert(a);
    for ( auto& x : s) {
        cout<<x;
    }
    // Xử lý
}